\section{Discussion and Limitations}
\label{sec:discussion}

\subsection{Pseudo-time Oscillations and Averaging Uncertainty in Steady RANS}
\label{sec:des_statistics}

Steady simpleFoam solves do not always converge to a fixed point: near the critical slant angle ($\alpha_\mathrm{slant}\approx28$--$36^\circ$), the separated wake exhibits low-frequency unsteadiness that the RANS pseudo-time march cannot damp out, producing a limit-cycle oscillation in the force coefficients even at solver convergence. All 79 completed cases were audited by computing the standard deviation of $C_d$ and $C_l$ over the 200-iteration averaging window (iterations 600--800). Results are summarised below.

For the majority of cases (those with attached or fully separated flow away from the critical regime) the pseudo-time oscillation is negligible: the median $\sigma_{C_d} = 0.003$ and median $\sigma_{C_l} = 0.015$ over the averaging window. However, 27 of 79 cases have $\sigma_{C_d} > 0.005$, indicating a limit-cycle regime. The worst offender is \texttt{case\_bo\_001} ($\alpha_\mathrm{slant}=35.7^\circ$, $\alpha_\mathrm{diff}=9.3^\circ$) with $\sigma_{C_d}=0.023$ and $\sigma_{C_l}=0.097$ — the BO iteration~1 point that returned $f_\mathrm{cfd}=0.642$. This case was verified to have a mesh of the same quality as the converged cases (${\sim}435$K cells, solver reaching \texttt{End}); the oscillation is a real flow-regime signature, not a numerical divergence. The high reported $f$ value is the mean of a non-stationary iterate rather than a well-converged aerodynamic prediction.

Three consequences follow. First, the minimum detectable $C_d$ difference between two stable cases is ${\sim}2\times\sigma_{C_d}/\sqrt{N_\mathrm{win}}\approx 2\times0.003/\sqrt{9}\approx0.002$; for the worst oscillating cases this degrades to ${\sim}0.015$. Second, $C_l$ uncertainty dominates: the median $\sigma_{C_l}=0.015$ translates to a minimum detectable downforce difference of ${\sim}0.010$ per case; but for the critical-angle band the $\sigma_{C_l}$ can reach $0.10$, making individual $C_l$ observations unreliable as GP training data there. Third, the correction GP's WhiteKernel noise term correctly absorbs this structured uncertainty: anchors~004, 005 and 007 (all of which show elevated $\sigma_{C_l}$ in the audit) received larger inferred noise variances during hyperparameter optimisation, widening the correction GP's predictive bands in the critical-angle region (\S\ref{sec:flat_optimum}).

The recommended fix for future campaigns is to replace steady simpleFoam with pimpleFoam (URANS) at the oscillating design points, using a physical time-averaging window of ${\sim}0.3$~s after initial transient decay. This would resolve the bistable flow mode directly and produce a well-defined time-mean rather than a pseudo-time average of an oscillation.

\subsection{EI Non-Convergence and the Flat Optimum}
\label{sec:flat_optimum}

The EI plateau at $\approx$0.005--0.01 across 20 BO iterations indicates a structural limitation of BO in this setting, not a failure of the acquisition function. The Expected Improvement scales as $\mathrm{EI} \propto \sigma(\mathbf{x}_*)$ near the optimum: if the GP has a long length scale (broad uncertainty) and a shallow objective landscape, each new observation reduces $\sigma$ locally but reveals comparable uncertainty nearby, maintaining a roughly constant EI.

In physical terms, the high-slant regime ($\alpha_\mathrm{slant} > 35^\circ$) produces a slowly varying vortex topology in the L2 surrogate landscape. The stencil study (\S\ref{sec:stencil}) reveals, however, that the true HF landscape is asymmetric: gentle slopes toward lower slant and lower diffuser angle, but sharp objective walls when moving toward higher values ($f_1$ rises 27--30\% within $1^\circ$ of the optimum in both the $+\alpha_s$ and $+\alpha_d$ directions). The L2 surrogate missed these walls because the high-slant, high-diffuser quadrant was sparsely sampled and the GP extrapolated smoothly. The BO therefore spread evaluations broadly across the transition region rather than exploiting the high-slant basin, which is consistent with the corrupted training point inflating uncertainty there. The HF landscape is sharply peaked in two directions — just not the directions the L2 surrogate revealed.

From a practical standpoint, EI non-convergence is not fatal: the GP surrogate correctly identified the global trend and confirmed case~021 ($\alpha_\mathrm{slant}=32.5^\circ$) as the incumbent through all 20 BO iterations. The MF co-Kriging correction (\S\ref{sec:mf_results}) then relocates the optimum accounting for the L2--L3 fidelity gap. A secondary benefit of the stencil study (\S\ref{sec:stencil}) is that it explains retrospectively why EI concentrated on the $33$--$38^\circ$ band: the corrupted BO iteration~1 training point ($f=0.642$ at $35.7^\circ$, a pseudo-time oscillation artefact) inflated GP uncertainty in the transition region, directing acquisition away from the true high-slant basin. With the L3 re-solve ($f_1=0.215$, well-converged) the surrogate landscape would have been smoother and EI exploitation of the $38$--$40^\circ$ region more efficient.

\subsection{Multi-Fidelity Correction and Optimum Verification}
\label{sec:verification_gap}

The initial single-fidelity campaign identified $\alpha_\mathrm{slant}=32.5^\circ$, $\alpha_\mathrm{diff}=8.9^\circ$ as the L2 RANS optimum ($f = 0.210$). As quantified in \S\ref{sec:l3_validation}, a single L3 validation run at this point revealed a fidelity gap of $\Delta C_l = +0.160$, inflating the L3 objective to $f = 0.248$ — an 18\% degradation. The root cause is the coarser L2 boundary layer delaying slant separation onset and artificially strengthening the trailing vortex system.

To close this gap, an additive co-Kriging correction was fitted to 8 L3 anchor simulations spanning the full design space (\S\ref{sec:l3_anchors}). The corrections $\delta_{C_l}$ at the anchor locations range from $-0.114$ (at $40.0^\circ/12.0^\circ$) to $+0.167$ (at $21.3^\circ/15.4^\circ$), with a mean of $+0.042$ and standard deviation $0.087$. The corrected HF surrogate relocates the objective minimum accounting for this spatially varying offset, and the resulting MF optimum is verified with a dedicated L3 solve (Table~\ref{tab:mf_optimum}, \S\ref{sec:mf_results}).

The key methodological insight is that the fidelity gap is not a constant offset: $\delta_{C_l}$ varies spatially with $\alpha_\mathrm{slant}$ because the degree of slant separation — and hence the L2 mesh's over-prediction of vortex strength — changes across the design space. Near the critical angle ($21.3^\circ$--$32.5^\circ$) the correction exceeds $+0.12$; at high slant ($38.9^\circ$) where the flow is fully separated the correction is only $+0.012$. A constant-bias correction (applying the single observed $\Delta C_l = +0.160$ at the old optimum everywhere) would mis-locate the corrected minimum by over-correcting in the high-slant basin where the MF optimum actually resides. The co-Kriging model learns this spatial variation from the 8 anchors and propagates it correctly to the re-optimisation.

With 8 anchor points in a 2-D space the correction GP is deliberately conservative: the WhiteKernel noise term absorbs anchor-to-anchor variation that may reflect physical flow-regime changes rather than GP model error. The predictive uncertainty $\sigma_\mathrm{HF}$ is therefore wider than the L2 surrogate alone. The verification L3 solve at the MF optimum ($\alpha_\mathrm{slant}=38.9^\circ$, $\alpha_\mathrm{diff}=10.5^\circ$) returned $f_1 = 0.1975$, against the surrogate prediction of $f_1 = 0.1964$ — a $0.6\%$ error, well within the predicted $\pm 2\sigma_\mathrm{HF}$ band. This confirms both the surrogate's fidelity at the optimum and the validity of the $+4.9^\circ$ diffuser shift as a genuine feature of the HF landscape rather than a surrogate artefact.

\subsection{RANS Turbulence Model Limitations}
\label{sec:rans_limits}

The $k$-$\omega$~SST model used for the RANS DoE is known to under-predict mixing in separated shear layers, producing the positive $\delta_{C_d}$ correction values observed in Table~\ref{tab:l3_anchors_results}. More critically, the RANS solution is steady-state: near the critical slant angle ($\alpha_\mathrm{slant} \approx 28$--$32^\circ$), the actual flow periodically switches between attached and separated states, and the steady RANS converges to an averaged intermediate state that does not correspond to either physical regime. This means RANS DoE data in the critical region is structurally mis-specified, not merely noisy, and the co-Kriging correction GP must learn a large, non-smooth $\delta$ in that zone.

The consequence for surrogate quality is that the correction GP's length scale in the slant direction is shorter near the critical angle, correctly increasing surrogate uncertainty there and widening the $\sigma_\mathrm{HF}$ band. The MF optimum's uncertainty is therefore physically motivated: the correction GP is uncertain precisely in the region where the RANS model is most unreliable.

\paragraph{Scope of the multi-fidelity correction.} It is important to note that the co-Kriging correction $\delta(\mathbf{x})$ spans \emph{mesh fidelity only}: the L3 anchors are solved with the same kOmegaSST steady-RANS closure as the L2 DoE cases. The turbulence-model error — systematic over-prediction of vortex strength in separated flow, and the inability of a steady solver to represent the bistable critical-angle regime — is \emph{common to both fidelity levels} and passes through the correction GP uncorrected. A further fidelity axis, using pimpleFoam URANS or DES at the anchor locations, would capture this component and is the natural next step in the multi-fidelity hierarchy. The present result should therefore be interpreted as the optimum of the \emph{L3 RANS landscape}, not of the true time-averaged flow.

\subsection{Objective Function Weighting and Optimum Migration}
\label{sec:lambda_sensitivity}

The objective $f_1 = C_d + \tfrac{1}{3}C_l$ assigns equal aerodynamic value to a drag count as to one-third of a downforce count. The choice $\lambda = 1/3$ is conventional for ground-vehicle drag-downforce trade-offs but is ultimately circuit-dependent: a high-downforce circuit increases the marginal lap-time value of $C_l$ (reducing $\lambda$), while a low-drag circuit does the opposite. To quantify how sensitive the optimum location is to this choice, the MF co-Kriging surrogate was re-optimised for six values of $\lambda$ spanning the full practical range. Results are reported in Table~\ref{tab:lambda_sweep} and Figure~\ref{fig:lambda_sweep}.

\begin{table}[h]
  \centering
  \caption{MF optimum location as a function of $\lambda$ (HF surrogate mean). All results from the fitted co-Kriging model; no additional CFD runs were performed.}
  \label{tab:lambda_sweep}
  \begin{tabular}{lccccc}
    \toprule
    $\lambda$ & $\alpha_s^*$ (°) & $\alpha_d^*$ (°) & $\hat{C}_d^*$ & $\hat{C}_l^*$ & $\hat{f}^*$ \\
    \midrule
    0 (drag only) & 15.4 &  9.5 & 0.266 & $-$0.006 & 0.266 \\
    1/6           & 39.0 & 10.5 & 0.288 & $-$0.275 & 0.242 \\
    1/3 (baseline, verified at L3) & 38.9 & 10.5 & 0.288 & $-$0.275 & 0.196 \\
    1/2           & 38.8 & 10.5 & 0.288 & $-$0.276 & 0.151 \\
    2/3           & 38.8 & 10.5 & 0.288 & $-$0.276 & 0.105 \\
    1             & 38.8 & 10.4 & 0.288 & $-$0.276 & 0.013 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\textwidth]{figures/lambda_sweep}
  \caption{MF surrogate optimum migration with objective weighting $\lambda$. Left: optimal slant angle. Centre: optimal diffuser angle. Right: $C_d^*$ and $C_l^*$ at the optimum. The vertical dashed line marks $\lambda=1/3$ (campaign baseline). For any $\lambda>0$ the optimum geometry is clustered near $(38.8$--$39.0^\circ,\ 10.4$--$10.5^\circ)$; only the pure drag case ($\lambda=0$) relocates the optimum to the low-slant regime. All values from the fitted HF co-Kriging surrogate.}
  \label{fig:lambda_sweep}
\end{figure}

A notable finding from the sweep is the robustness of the optimum location: for any $\lambda > 0$ (any positive weighting of downforce), the MF surrogate predicts the same geometric optimum near $(38.8$--$39.0^\circ,\ 10.4$--$10.5^\circ)$. Only the pure drag-minimisation case ($\lambda=0$) relocates the optimum to the low-slant regime ($15.4^\circ$, near-zero $C_l$). This confirms that the $\lambda = 1/3$ result is not sensitive to the exact weighting choice in any practical motorsport context. For the full Pareto-front perspective — trading drag against downforce without a fixed $\lambda$ — see Figure~\ref{fig:pareto_front}, which shows 61 non-dominated designs across the full range of drag--downforce trade-offs available in the 2D design space.

\subsection{Simplified Geometry Relative to Full Race Car}
\label{sec:ahmed_limits}

The Ahmed body is a canonical benchmark, not a race car. Three simplifications introduce limitations when interpreting results in a motorsport context:

\textbf{No moving ground plane.} The simulations use a stationary ground boundary condition, consistent with the Lienhart \& Becker \cite{lienhart2002} wind-tunnel setup. Real ground-effect flows require a moving ground (tangential velocity matching $U_\infty$), which strengthens the underbody venturi effect and changes the separation behaviour at the diffuser exit. The stationary ground underestimates diffuser efficiency.

\textbf{No rotating wheels.} Wheel rotation generates significant additional drag and modifies the ground plane boundary layer upstream of the diffuser. The cylindrical leg geometry used here is a simplified support structure, not a rotating tyre.

\textbf{No cooling flows.} Real ground vehicles pass significant mass flow through radiators, which exits through body outlets and interacts with the underbody and rear wake. The Ahmed body is solid.

These simplifications mean that the absolute coefficient values ($C_d \approx 0.27$--$0.29$ at the optimum) are not directly transferable to a race car. The optimisation trends and the MF framework itself generalise fully to more complex geometries.
